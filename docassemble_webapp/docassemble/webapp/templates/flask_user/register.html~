{% extends 'flask_user/public_base.html' %}

{% block content %}
{% from "base_templates/form_macros.html" import myrender_field, myrender_submit_field, myrender_hidden_field %}

<!--{% from "flask_user/_macros.html" import render_field, render_submit_field %}-->
<div class="page-header">
  <h1>{%trans%}Register{%endtrans%}</h1>
</div>

{% if user_manager.enable_register %}
    <p>{%trans%}Already registered?{%endtrans%} <a href="{{ url_for('user.login') }}" tabindex='290'>{%trans%}Sign in.{%endtrans%}</a></p>
{% endif %}

<form action="" method="POST" novalidate formnovalidate class="form-horizontal" role="form">
    {{ form.hidden_tag() }}

    {# Username or Email #}
    {% set field = form.username if user_manager.enable_username else form.email %}
    <!--<div class="control-group {% if field.errors %}has-error{% endif %}">-->
    {# Label on left, "Already registered? Sign in." on right #}
    <div class="control-group">
	<label for="{{ field.id }}" class="control-label">{{ field.label.text }}</label>
	<div class="controls">
	    {{ field(class_='form-control', tabindex=210) }}
	    {% if field.errors %}
		{% for e in field.errors %}
		    <p class="help-block">{{ e }}</p>
		{% endfor %}
	    {% endif %}
	</div>
    </div>
    {% if user_manager.enable_email and user_manager.enable_username %}
	{{ myrender_field(form.email, tabindex=220) }}
    {% endif %}

    {{ myrender_field(form.first_name, tabindex=240) }}

    {{ myrender_field(form.last_name, tabindex=250) }}

    {{ myrender_field(form.password, tabindex=260) }}

    {% if user_manager.enable_retype_password %}
	{{ myrender_field(form.retype_password, tabindex=270) }}
    {% endif %}
    {{ myrender_hidden_field(form.social_id, value=random_social()) }}
    {{ myrender_submit_field(form.submit, tabindex=280) }}
    <!--</div>-->

</form>

{% endblock %}
