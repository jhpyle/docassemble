{% extends 'flask_user/public_base.html' %}

{% block content %}
{% from "base_templates/form_macros.html" import render_field, render_submit_field %}

<h1>User Profile</h1>

<ul class="nav nav-pills"><li><a href="{{ url_for('user.change_password') }}">Change password</a></li></ul>

<!--{% from "base_templates/form_macros.html" import render_field, render_submit_field %}-->
<form action="" method="POST" role="form" class="form">
  <!--<div class="row">
    <div class="col-sm-6 col-md-5 col-lg-4">-->
    {{ form.hidden_tag() }}

    {{ render_field(form.first_name, tabindex=240) }}

    {{ render_field(form.last_name, tabindex=250) }}
    <br>
    <div class="form-actions">
      {{ render_submit_field(form.submit, tabindex=280) }}
    </div>
</form>

{% endblock %}
